<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Support — {{product.name}}</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; max-width: 720px; margin: 0 auto; padding: 2rem; color: #1a1a1a; line-height: 1.7; }
    h1 { font-size: 1.75rem; margin-bottom: 0.25rem; }
    h2 { font-size: 1.25rem; margin-top: 2rem; }
    .meta { color: #666; font-size: 0.9rem; margin-bottom: 2rem; }
    a { color: #0066cc; }
  </style>
</head>
<body>
  <h1>Support</h1>
  <p class="meta">{{product.name}} &mdash; Help &amp; Contact</p>

  <h2>Getting Help</h2>
  <p>If you need assistance with {{product.name}}, here are your options:</p>

  <h2>Email Support</h2>
  <p>Send us an email at <a href="mailto:{{product.contactEmail}}">{{product.contactEmail}}</a>. We aim to respond within 48 hours.</p>

  <h2>Frequently Asked Questions</h2>
  <p>{{product.faqSummary}}</p>

  <h2>Bug Reports</h2>
  <p>If you encounter a bug, please email us with a description of the issue, what you expected to happen, and the device/browser you are using.</p>

  <h2>Feature Requests</h2>
  <p>We welcome feedback. Email us your suggestions at <a href="mailto:{{product.contactEmail}}">{{product.contactEmail}}</a>.</p>
</body>
</html>
